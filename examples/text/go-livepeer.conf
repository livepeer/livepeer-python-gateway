# go-livepeer config files use the same keys as CLI flags in `key value` form.
orchestrator true
useLiveRunners true
httpAddr http://localhost:8935
serviceAddr http://localhost:8935
orchSecret abcdef
v 99

# Point this at the static runner config below instead of registering from Python.
liveRunnerConfig runners.json
